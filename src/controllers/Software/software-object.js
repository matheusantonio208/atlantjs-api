/* eslint-disable no-unused-vars */
export function softwareObject(
  data,
  {
    _id,
    name,
    description,
    url_icon,
    compatible_os,
    access_link,
    command_download,
    category,
    url_page,
    linked_layers,
  } = data,
) {
  return data;
}
